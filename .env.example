# HuggingFace Token (required for downloading the model)
# Get it from: https://huggingface.co/settings/tokens
# Accept model license: https://huggingface.co/pyannote/speaker-diarization-3.1
HF_TOKEN=your_huggingface_token_here

# Diarization Service Authentication
# Generate with: openssl rand -hex 32
DIARIZATION_TOKEN=your_secure_random_token_here

# Service Configuration
DIARIZATION_HOST=127.0.0.1
DIARIZATION_PORT=5000

# Model Storage Path (optional - if not set, uses HuggingFace cache)
# Must point to the speaker-diarization-3.1 directory, not just pyannote
PYANNOTE_MODEL_PATH=./models/pyannote/speaker-diarization-3.1

# Proxy Configuration (optional)
# Uncomment and set if you need to use proxy for downloading models
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# Max file size in bytes (default: 100MB)
MAX_CONTENT_LENGTH=104857600
